<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lista de Roupas</title>
  <style>
    body {
      background-color: black; 
      color: white; 
      margin: 0;
      font-family: sans-serif;
    }

    h2 {
      color: #FFFFFF; /* Título com texto branco */
      text-align: center;
      margin-bottom: 20px;
    }

    table {
      width: 90%;
      border-collapse: collapse;
      margin: 0 auto;
      background-color: #E6E6FA; /* Fundo lavanda da tabela */
      border-radius: 10px;
      box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
    }

    thead tr {
      background-color: #78588f; /* Fundo roxo do cabeçalho */
      color: white;
    }

    tbody tr {
      text-align: center;
      border-bottom: 1px solid #ddd;
      background-color: #444; /* Fundo cinza escuro das linhas */
      color: white;
    }

    tbody tr:hover {
      background-color: #555; /* Efeito hover para linhas */
    }

    th, td {
      padding: 12px;
      text-align: center;
    }

    a {
      text-decoration: none;
      background-color: #6f3e79;
      color: white;
      padding: 5px 12px;
      border-radius: 4px;
    }

    a:hover {
      background-color: #4B0082; /* Cor mais escura ao passar o mouse */
    }

    button {
      background-color: #c4503c;
      color: white;
      padding: 6px 12px;
      border: none;
      border-radius: 4px;
    }

    button:hover {
      background-color: #a6382b; 
    }

    .add-button {
      display: block;
      text-align: center;
      margin: 20px auto; /* Centralizado no site */
      width: 200px; 
      background-color: #4B0082;
      color: white;
      padding: 10px; /* Menor altura */
      border-radius: 6px;
      text-decoration: none;
    }

    .add-button:hover {
      background-color: #6A5ACD; /* Hover no botão adicionar */
    }
  </style>
</head>
<body>
  <h2>Lista de Roupas</h2>

  <table>
    <thead>
      <tr>
        <th>Nome</th>
        <th>Tamanho</th>
        <th>Cor</th>
        <th>Preço</th>
        <th>Quantidade</th>
        <th>Ações</th>
      </tr>
    </thead>
    <tbody>
      {{#each clothes}}
      <tr>
        <td>{{this.nome}}</td>
        <td>{{this.tamanho}}</td>
        <td>{{this.cor}}</td>
        <td>R$ {{this.preco}}</td>
        <td>{{this.quantidade}}</td>
        <td>
          <a href="/clothes/add/{{this._id}}">Editar</a>
          <form action="/clothes/delete/{{this._id}}" method="POST" style="display:inline;">
            <button type="submit">Deletar</button>
          </form>
        </td>
      </tr>
      {{/each}}
    </tbody>
  </table>

  <a href="/clothes/add" class="add-button">Adicionar Roupa</a>
</body>
</html>
